<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Autenticación de 2 Factores</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            font-family: 'Franklin Gothic Medium', 'Arial Narrow', Arial, sans-serif;
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 100vh;
            background-color: #aaffe3;
        }
        h1 {
            color: #333;
        }
        h3 {
            color: #666;
        }
        form {
            margin-top: 20px;
            display: flex;
            flex-direction: column;
            align-items: center;
        }
        label {
            margin-bottom: 10px;
        }
        input[type="text"] {
            padding: 10px;
            margin-bottom: 20px;
            border-radius: 5px;
            border: 1px solid #ccc;
            width: 300px;
        }
        /* Eliminamos estilos de botón repetidos */
        /* button {
            padding: 10px 20px;
            background-color: #007bff;
            color: #fff;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }
        button:hover {
            background-color: #0056b3;
        } */
    </style>
</head>
<body>
    <h1>Autenticación de 2 Factores</h1>
    <h3>Escanea el QR</h3>
    <img src="<%= qrCode %>" alt="QR Code">
    <form action="/verify" method="post">
        <input type="hidden" name="secret" value="<%= secret %>">
        <label for="token">Ingresa el OTP:</label>
        <input type="text" id="token" name="token" required>
        <!-- Agregamos clases de Bootstrap para estilizar el botón -->
        <button type="submit" class="btn btn-outline-dark">Verificar</button>
    </form>


    
</body>
</html>
